# requirements

## 功能概述（<=150字）
系统在 PC 仿真环境实现机器狗多模态实时交互 MVP：接入视频与音频流，完成视觉属性识别、流式语音识别、多模态对话生成、语音播报与动作意图输出，并具备基础合规提示与故障降级能力。

## 用户故事（EARS）
- 当用户进入摄像头与麦克风范围时，系统应当在单轮内综合视觉与语音信息给出自然回复。
- 当视觉模块检测到低置信度或识别失败时，系统应当退化为仅语音对话并向用户说明当前能力受限。
- 当用户询问与“颜值/性格”相关内容时，系统应当先给出“仅供娱乐与研究”的免责声明，再输出回答。
- 当对话内容触发不安全或不当表达风险时，系统应当拦截并替换为中性、安全话术。

## 验收标准（EARS）
- 当系统在标准环境运行时，应当在 1.5s~3s 内完成单轮端到端响应（输入到播报开始）。
- 当任一单模块异常退出时，系统应当保持主循环可用并记录结构化错误日志。
- 当系统处理单人场景、多人场景和噪声场景时，应当连续完成实时对话且不崩溃。
- 当动作策略输出动作意图时，系统应当至少支持 3 种可执行动作（mock）并记录执行结果。

## 性能 / 安全 / 合规约束
- 性能：MVP 目标端到端时延 1.5s~3s，优先保障流畅对话。
- 稳定性：所有核心模块必须具备超时、异常捕获与降级路径。
- 合规：涉及“颜值/性格”等高风险表达时必须附加免责声明。
- 安全：禁止将模型输出包装为医学、心理或专业结论；日志需可审计。

## 不在范围内（Out of Scope）
- Unitree 真机 SDK/ROS 的完整接入与硬件联调。
- Jetson 上的 TensorRT 深度优化与部署流水线。
- 生产级权限体系、账号系统与云端多租户能力。
- 医学/心理测评类正式产品化能力。
