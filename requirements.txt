# 核心 Web 框架（已固定版本）
fastapi==0.136.0
pyyaml==6.0.3
uvicorn==0.44.0

# ASR：FunASR 固定已验证版本，避免上游 breaking change
funasr==1.3.1

# LLM：Transformers + AutoAWQ（量化推理）
# transformers 5.x 为首个大版本，固定以避免 API 变更
transformers==4.51.3
# autoawq 无稳定版本号约束，固定已知可用版本
autoawq==0.2.8

# TTS（CosyVoice 通过 pip 安装，版本依赖上游，此处仅做注释说明）
# 如需固定版本，建议从官方发布页获取 wheel 后本地安装：
# pip install cosyvoice==<version>
cosyvoice

# 视觉
numpy==2.2.5
opencv-python==4.11.0.86
onnxruntime==1.21.0
insightface==0.7.3
ultralytics==8.3.144
# mmpose 无 PyPI 正式发布，通过 mim 安装；此处保留记录
mmpose
