version: "3.9"

services:
  lucky-dog-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./configs:/app/configs:ro
    environment:
      - PYTHONUNBUFFERED=1
